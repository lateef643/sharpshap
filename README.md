CicoServe Payments Agent Admin platform built with React JS.

This project uses:

SCSS. 

CSS modules (one local CSS file per component). 

Redux for state management. 

Redux thunk for asynchronous action dispatches. 

Axios JS. 